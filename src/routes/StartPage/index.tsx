import { StartFinish } from "@/components";
const StartPage = () => (
  <StartFinish
    titleComponent={<>Who wants to be a millionaire?</>}
    linkText={"Start"}
  />
);

export default StartPage;
